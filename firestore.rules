rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /antecedentes/{docId} {
      allow read: if true;   // lectura pública
      allow write: if request.auth != null; // solo escritura autenticada
    }
  }
}